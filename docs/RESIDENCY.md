# どのモードで、いつ何がロード/解放されるか — 常駐リファレンス

**日本語** | [English](RESIDENCY.en.md)

モード・量子化・turbo・TE配置の組み合わせが増えたため、**「この設定のこの局面で GPU に何が載っているか」**を一枚で引けるようにしたもの。すべて `core/runner.py` のコードから導き、実測ログで裏を取っている。

> **このドキュメントを作った理由**: 2026-08-09、ピーク VRAM の内訳を「transformer + デコード時のVAE」と説明したが**誤りだった**(実際は transformer をデコード前に解放している)。組み合わせが多すぎて記憶で答えると間違える、という実例。以後はここを見ること。

---

## 0. まず結論 — 代表的な2構成

> **測定環境は2台**: **96GB機** = RTX PRO 6000 Blackwell 96GB + 増設 RTX 4060 Ti 16GB、
> **48GB機** = RTX PRO 5000 Blackwell 48GB + RTX 4000 SFF Ada 20GB。以下、どちらの箱の
> 実測かを必ず明記する(README 冒頭の注記と同じ区分)。

### ① `H3_LOWVRAM=1` + TE を2枚目GPUへ(解放あり) — 48GB機

```bash
H3_LOWVRAM=1 H3_TE_PRUNE=1 H3_TE_DEVICE=cuda:1
```

| 局面 | GPU0 (48GB) に載っているもの | GPU1 (20GB) |
|---|---|---|
| アイドル | (ほぼ空、~1.5GB) | **TE-nf4 17.4GB(常駐しっぱなし)** |
| エンコード | — | TE-nf4 17.4GB |
| **デノイズ** | **transformer-int8 34.3GB + 活性化 ~6.6GB = 40.9GB** ← **ピーク** | TE-nf4 |
| (transformer 解放) | 0.3GB | TE-nf4 |
| デコード | vae ペア 11.3GB + バッファ | TE-nf4 |
| 終了時 | vae を CPU へ → 1.2GB | TE-nf4(解放しない) |

**重要**: デノイズとデコードは**時間的に重ならない**。`H3_KEEP_TRANSFORMER=0`(既定)では transformer はデコードの直前に必ず解放される(`generate()` のデコード位相直前の `_free_transformer()`)。ピーク 40.9GB は**デノイズ時**のもの。

### ② int8 単騎 + 解放停止(2026-08-12 の実用最適) — 96GB機で実測

```bash
H3_TRANSFORMER_QUANT=int8 H3_KEEP_TRANSFORMER=1 H3_VIDEO_VAE_FP16=1 \
  H3_TE_PROJ=NicoLab28/ClipProj-MiniMax-H3 H3_TURBO_LORA=1
```

**GPU 1枚**で transformer(int8)と投影TE(NF4)を**両方とも常駐させたまま**、デコード窓でも
何も解放しない構成。`H3_LOWVRAM` は使わない(plain)。

| 局面 | GPU0 に載っているもの |
|---|---|
| アイドル(リクエスト間) | transformer-int8 34.0GB + 投影TE NF4 3.11GB(どちらも常駐) |
| エンコード | 同上(TE を解放しない) |
| デノイズ | 同上 + 活性化 |
| デコード | 同上 + fp16 デコード(**transformer を解放しない**) |
| 終了時 | vae を CPU へ。**何も解放しない** |

**実測ピーク**(96GB機、turbo 4steps、768²、定常値): t2i 45.0GB / **t2va 5秒 45.6GB** /
ref2i 45.4GB / i2va 45.9GB。**どの位相がピークかは未計測**(①のような位相分解はしていない)。
48GB 級の実効予算 ~49.8GB に対し余裕 ~4.2GB で、**48GB カード1枚に収まる見込み**
(**48GB 実機での確認は未実施** — 96GB機で測ったピークが 48GB 予算に収まることの確認まで)。
速度・条件の詳細は §5.5。

---

## 1. 何が「常駐」で何が「毎回」か — 4つの軸

判断は次の4つの独立した軸で決まる。混乱の元はこれらが直交していること。

| 軸 | 環境変数 | 効果 |
|---|---|---|
| **A. どこまで常駐させるか** | `H3_LOWVRAM` (0/1/group) | 最重要。下記 §2 の表を決める |
| **B. TE をどのGPUに置くか** | `H3_TE_DEVICE` | 設定すると TE は**一切解放されなくなる**(別GPU常駐) |
| **C. ロードを速くするか** | `H3_TE_PREQUANT` | 「いつロードするか」は変えない。**ロードの中身だけ**が速くなる |
| **D. 解放そのものを止めるか** | `H3_KEEP_TRANSFORMER` | **速度への効果は最大**(同条件で 2.6倍)。A の設定に**上書き**して、transformer の解放を全部やめる(§5.5)。`group` 以外のどの `H3_LOWVRAM` 値とも組める |

なお **`H3_TE_PROJ`(投影TE)は軸 B の代替でもある**: TE が 3.11GB(NF4)まで小さくなるので、
別GPUに逃がさなくても計算用GPUに同居できる。軸 D のガードも `H3_TE_DEVICE` と同格に扱う。

**turbo (`H3_TURBO_LORA` / リクエストの `turbo=1`) はこの表に影響しない。** LoRA は transformer にラッパーを被せるだけで、常駐の増減は起きない(初回のみ 1.38GB の重み読み込みとラップ処理)。FBC が強制OFFになるのと、既定ステップ数が変わるだけ。

---

## 2. `H3_LOWVRAM` 別・フェーズ×常駐物

### `H3_LOWVRAM=0` + `H3_TE_QUANT=bnb-4bit`(96GB級の既定)

TE と transformer が**両方とも常駐しっぱなし**。VAE だけがフェーズごとに GPU を往復する。

| 局面 | 常駐 | 備考 |
|---|---|---|
| 定常(リクエスト間) | transformer 66.3GB + TE-nf4 21GB = **87.5GB** | VAE は CPU |
| エンコード | 同上 | |
| デノイズ | 同上 + 活性化 | |
| **デコード** | **transformer を解放** → vae ペア 11GB | 3つ同時は 98.5GB で 96GB を超えるため |
| デコード後 | transformer を再ロード(定常へ復帰) | |

> **plain モードでもこの解放は止められる(2026-08-12)**: 上の「デコードで transformer を
> 解放 → 直後に再ロード」は **実測 11.9〜12.3s/リクエスト**の固定費で、96GB機の bf16 plain
> 構成が int8 常駐構成に負けていた原因だった。この解放は「TE-nf4 21GB + transformer bf16
> 66.3GB + VAE fp32 11GB = 98.5GB > 96GB」という **32B TE を GPU0 に置く前提**の収支から
> 来ており、TE が GPU0 に居ない構成(`H3_TE_DEVICE` で外出し、または 3.11GB の投影TE)では
> 前提が成立しない: 66.3 + fp16 デコード 11.4 = **77.7GB**(投影TE を同居させても +3.11)。
> `H3_KEEP_TRANSFORMER=1` の import 時ガードを緩めて plain モードにも適用できるようにした
> (§5.5)。**注意**: bf16 で止める場合は 77.7GB なので実質 80GB 級以上が要る。

### `H3_LOWVRAM=1`(48GB級)

**リクエスト間は何も常駐しない**のが原則。ただし `H3_TE_DEVICE` を設定すると TE だけは例外。

| 局面 | `H3_TE_DEVICE` 未設定 | `H3_TE_DEVICE=cuda:1` |
|---|---|---|
| 入口 | transformer を解放(前回の残り) | 同左 |
| エンコード | **TE をロード** 17.4GB | TE は GPU1 に常駐済み(**ロード不要**) |
| layout/latents/timesteps | TE 常駐のまま | TE をパイプから外し transformer をロード |
| (TE 解放) | **TE を解放** | **解放しない** |
| デノイズ | transformer-int8 34.3GB + 活性化 | 同左 |
| (transformer 解放) | **解放** | **解放**(現状) |
| デコード | vae ペア 11.3GB | 同左 |
| 出口 | vae を CPU へ。**何も再ロードしない** | 同左 |

**毎リクエストの固定費**:

| | TE ロード | transformer ロード | 合計 |
|---|---|---|---|
| 素の構成 | 46.5〜55.8s | 32.5s | **~85s** |
| + `H3_TE_PREQUANT=1` | 21〜34s | 32.5s | **~55s** |
| + `H3_TE_DEVICE=cuda:1` | **0s** | 14.8〜32.7s | **~25s** |

**`H3_KEEP_TRANSFORMER=1` を重ねた場合**(`H3_TE_DEVICE` **または** `H3_TE_PROJ` のどちらかが
必要、詳細は §5.5): 上の表の「(transformer 解放)」行と「デコード」行が変わる — transformer は
**デコード位相でも解放されず**、**リクエストをまたいでも常駐したまま**になる(「入口」の解放も
発生しない)。常駐し続けるのは初回ロードの1回のみで、transformer ロードの固定費
(14.8〜32.7s)は初回だけに収束する。デコード位相のピークは transformer 34.3GB 常駐 +
fp16 デコード ~11.4GB(`H3_VIDEO_VAE_FP16=1` 必須)。実測は §5.5 参照。

### `H3_LOWVRAM=group`(24-32GB級)

transformer は**ホストRAMに常駐**し、ブロック単位(1〜2個、~1.4GB)で GPU を往復する。

| 局面 | GPU上 | 備考 |
|---|---|---|
| 起動時 | transformer は CPU 常駐(int8, group-offload フック付き) | プロセス生存中ずっと |
| エンコード | TE-nf4 21GB | |
| デノイズ | TE-nf4 21GB + transformer の 1-2 ブロック ~1.4GB | |
| **デコード** | **TE を強制解放** → vae ペア 11GB | TE(21GB)+デコード(16.3GB)=37GB が 32GB を超えるため |
| デコード後 | TE を再ロード | 次リクエストが TE を先に要るため |

**注意**: このモードだけ「デコードで解放されるのは transformer ではなく TE」。group-offload された transformer の GPU 占有はもともと小さいので、譲るのは TE の側になる。

> **group の解放は _host_emptyCache が要る(2026-08-12 追加)**: group offload は int8 重み
> ~34GB を **pinned host memory** に置く。`_free_transformer` / `_free_transformer_ref` の
> del+gc では torch のホスト側キャッシングアロケータにページが残り OS に返らない
> (`torch.cuda.empty_cache()` はデバイス側のみ)。**ホスト側 pinned キャッシュはデバイス
> 解放とは別勘定**であり、これを返さないと t2va→ref2va のモード切替が RAM ガードで拒否される
> (解放後 avail 38.6GB / RssShmem 45.6GB 残留 → 後続 transformer_ref ロードが 40GB 要求で
> 失敗)。修正で group モード時のみ `torch._C._host_emptyCache()`(私有APIのため getattr
> ガード付き)を追加し、解放直後に avail が **85.8GB** へ完全回復する。詳細は
> [docs/internal/TECHNICAL_REPORT.md](internal/TECHNICAL_REPORT.md) の追補 B8。

---

## 3. `H3_TE_DEVICE` を設定したときの追加の約束

- TE は **`_free_text_encoder()` を呼んでも解放されない**(`force=True` でも)。解放しないことが目的だから
- TE は**普段パイプから外れている**。`_te_attached()` の窓(エンコード中)だけ繋がる
  - 理由: `_execution_device` は components 順で最初の nn.Module のデバイスを返すため、繋ぎっぱなしだと layout も **decode も**別GPUにテンソルを作って落ちる(実機で再現)
- layout/latents/timesteps の窓では `_pin_execution_device_to_compute()` が **vae も一時的に外す**(TE を外した次に見つかるのが CPU 上の vae になり、今度は `cpu` が返るため)
- **ref2va は TE用GPUが 24GB 未満なら 400 で拒否**される(20GB で OOM を実測済み)。
  参照バッチ(`/api/ref2i_batch` / `/api/ref2va_batch`)も同じガードで拒否される

> **この 24GB 閾値は要見直し(未修正)**: `_te_external_usable_for("ref2va")` は
> **TE用GPUの総容量が 24GB 以上か**しか見ておらず、載っている TE が 32B なのか投影4B なのかを
> 区別しない。閾値の根拠は「32B TE(17.45GB)+ 2048px 短辺の vision 活性化 3.22GB 以上」で、
> **3.11GB の投影TE には過大**。実害として、96GB機の2枚目(4060 Ti 16GB)に投影TEを常駐させた
> 構成では **`H3_TE_DEVICE` と参照系(単発・バッチとも)が併用できない**。投影TEの vision 経路
> 自体は 2026-08-11 に目視検証済みで、動くかどうかの問題ではなく**ガードの判定基準の問題**。

---

## 4. よくある誤解

| 誤解 | 実際 |
|---|---|
| 「ピーク 40.9GB は transformer + デコードVAE」 | **違う**。ピークはデノイズ時(transformer 34.3 + 活性化 6.6)。デコード時は transformer 解放済みで VAE 11.3GB |
| 「`H3_TE_PREQUANT` で TE が常駐するようになる」 | **違う**。ロードするタイミングは変わらない。**ロードが速くなるだけ**(53s→29.5s) |
| 「turbo にすると VRAM が増える/減る」 | ほぼ変わらない。LoRA はラッパーで、初回に 1.38GB を読むだけ |
| 「`H3_LOWVRAM=group` は 1 より常に省メモリで遅い」 | 省メモリは正しいが、**静止画では group の方が遅い**(ブロック転送が固定費で、計算が軽い超短尺では転送が支配する) |
| 「TE を別GPUに置けば固定費がゼロになる」 | TE 分だけ。**transformer のロード(14.8〜32.7s)は残る**(デコード前に解放しているため)。ただし `H3_KEEP_TRANSFORMER=1` を重ねればこれも初回のみに収束する(§5.5) |
| 「`H3_KEEP_TRANSFORMER` は `H3_LOWVRAM=1` + `H3_TE_DEVICE` が必須」 | **もう違う(2026-08-12 に緩和)**。必要なのは「`H3_LOWVRAM` が `group` でないこと」「`H3_TE_DEVICE` **または** `H3_TE_PROJ`」「`H3_VIDEO_VAE_FP16=1`」の3つ。plain(`H3_LOWVRAM=0`)でも、TE をGPU0に同居させる単騎構成でも成立する(§5.5) |
| 「TE の置き場所で速度が2倍違う」 | **解放が残っていた時代の現象**。解放を止めた後は、単騎(TE も GPU0)7.08s vs 2枚(TE@GPU1)6.89s で差は 2.7%(96GB機・bf16・t2i 定常)。効いていたのは TE の置き場所ではなく**解放停止そのもの** |

---

## 5. VRAM 容量が変わったら — 表を覚え直さず「導出」する

**モードは VRAM 容量の関数**である。GPU を替えたら下記の部品表と不等式から再導出すること
(この表自体、96GB 機で作られ 48GB 機で作り直した経緯がある)。

### 5.1 部品表(すべて実測)

| 部品 | サイズ |
|---|---|
| text_encoder bf16 | 66.71GB(51層削除で 53.06GB) |
| **text_encoder nf4** | **21.02GB(51層削除で 17.45GB)** |
| transformer bf16 | 66.3GB |
| **transformer int8** | **34.0GB** |
| transformer_ref bf16 / int8 | 61.7GB / ~34GB |
| vae + audio_vae (fp32) | 11.0GB |
| デノイズ活性化 | ~5〜6.6GB(768²・5秒で実測 6.6GB) |
| **デコードのピーク** | **16.29GB**(video VAE fp16 なら ~11.4GB)。**この2つは 2026-08-10 の uint8 修正より前の値**(値の系譜は下記) |
| ref2va の参照エンコード追加分 | TE に対して **+3.2GB 以上**(2048px 短辺の vision tower。実測の下限) |
| **CUDA コンテキスト等(非PyTorch)** | **~1GB**(忘れやすい。下記の罠を参照) |

> **デコード位相の逆正規化を CPU 化した(2026-08-12)**: 上流 `MiniMaxH3VideoDecodeStep`
> の末尾 `(video.float() * pixel_std + pixel_mean).clamp(0,1)` は fp16 の全長デコード結果を
> GPU 上で一括 fp32 化していた(768²・124フレームで **838MiB** の一時テンソル)。runner 側
> サブクラス `_cpu_norm_video_decode_step()` で **fp16 のまま CPU へ移してから逆正規化**する
> ように変更し、この全長 fp32 一時テンソルが GPU から消えた(要素毎演算なので CPU/GPU で
> ビット同一 = 同一 seed PNG MD5 一致で実証)。全経路(t2va/t2i/ref2va/バッチ)に無条件適用。
> 上のデコードピーク 16.29GB / fp16 11.4GB は**この修正前の値**であり、修正後はここから
> 全長 fp32 数本ぶんが下がる。**修正後の再計測は未実施**なので、本ドキュメントの導出は
> すべて修正前の値(=保守側)のまま行っている — 実際にはもう少し余裕がある。
> 8GiB×2 で t2va が入るようになった直接の条件。
> 詳細は [docs/internal/TECHNICAL_REPORT.md](internal/TECHNICAL_REPORT.md) の追補 B6。

> **デコードピークの値の系譜(混同注意)**: この文書には3世代の値が混在している。
> ① **16.29GB(fp32)/ ~11.4GB(fp16)** = 2026-08-10 の uint8 中間テンソル修正より**前**。
> §5.1 の部品表・§5.2 の不等式・§5.5 の `H3_KEEP_TRANSFORMER` 成立導出はこの世代の値を使う。
> ② **14.2GB(fp32)/ 7.53GB(fp16)** = uint8 修正**後**。§5.3 の容量別テーブルはこちらを使う。
> ③ 2026-08-12 の逆正規化 CPU 化**後** = **未計測**。可否の導出はすべて①か②(=保守側)の
> ままなので、判定が甘くなる方向のずれは起きない。**新しい構成の可否を語るときは、どの世代の
> 値を使ったかを必ず書くこと。**

> **単位の罠(この表を作る過程で実際に間違えた)**: `nvidia-smi` は **MiB**、PyTorch の
> OOM メッセージは **GiB**、本アプリのログ(`gpu_mem_gb()`)は **GB(10進)**。
> 20GB カードは `nvidia-smi` で 20475 MiB = **21.47 GB(10進)** だが、PyTorch から見える
> 容量は 19.55 GiB = **20.99 GB(10進)**。**約 0.5GB の差**があり、さらに非PyTorch分
> ~1GB が引かれる。**カタログ容量をそのまま予算にすると 1.5GB ほど過大評価する**
> (この見落としで「ref2va は 20GB でも入る」と誤って導出しかけた。実測は OOM)。

### 5.2 満たすべき不等式(局面ごとに独立)

同時に載る必要があるのは**各局面の中だけ**。局面をまたいで合計する必要はない。

```
実効予算 = カタログ容量 − 単位差(~0.5GB) − CUDAコンテキスト等(~1GB)

エンコード : TE                                    ≤ 実効予算
デノイズ   : transformer + 活性化(~6.6GB)          ≤ 実効予算
デコード   : デコードピーク(16.29 / fp16なら11.4)   ≤ 実効予算
```

**リクエスト間で常駐させたいものがあれば、その分を各局面に足す。** ここが設計の分岐点になる。

- TE を常駐させたまま デノイズ したい → `TE + transformer + 活性化 ≤ 容量`
- transformer を常駐させたまま デコード したい → `transformer + デコードピーク ≤ 容量`

**この式はコードが実際に使っている**(2026-08-12)。`core/runner.py` の
`_effective_vram_budget_gb()` が上の予算式を、`_residency_requirements_gb()` が上の3本の
不等式の左辺を実装しており、`H3_KEEP_TRANSFORMER=1` の import 時ガードと
`core/settings.py` の `apply_reload_settings()` が両方ともこれで判定する。以前は
「48GB級では fp32 デコードが入らない」という**特定の箱の結論を定数で直書き**していたため、
容量の大きい箱でも fp16 デコードを強制していた。回帰確認は
`scripts/probe_residency_budget.py`(下の収支表の各行を入力に、可否が表と一致することを見る)。

#### 統合メモリ機 (GB10 / DGX Spark) での補正

`is_integrated` なデバイスでは **VRAM と RAM が同一プール**なので、上の式をそのまま適用すると
破綻する。`torch.cuda.get_device_properties().total_memory` が**システムメモリ全体**
(GB10 で 128.45GB) を返すため、ホスト側に居るもの — CPU にパークした VAE 対 ~11GB、
プロセス自身、ページキャッシュ — と二重計上になる。`_effective_vram_budget_gb()` は
このとき `MemAvailable` 側でも上限を掛ける(実測: total 基準なら 126.95GB になるところ、
`MemAvailable` 基準で **119.30GB**)。

**swap ゼロの箱では OOM killer に猶予がない**。実際、2026-08-12 の初回起動は
transformer bf16 66.28GB を「GPU」へ置いた時点で avail が 120.8→54.7GB へ落ち、
続く 32B TE の NF4 化ロードが収まらず強制終了した。なお上の3本の不等式は**定常状態の
位相**を見るものなので、**初回ロード時の量子化ピークは対象外**である点に注意
(32B TE の初回ロードは `models/prequant/` のキャッシュが無い間だけフル経路を通る)。
これに対しては2つ手を打った (2026-08-12):

- `preload_all()` が **TE を transformer より先に**ロードするようにした。逆順だと
  TE の初回量子化に残り 54.7GB しか渡らないが、この順なら 119GB 空いた状態で通る。
  定常状態の合計は順序によらず 87.3GB で変わらない。
- `_preflight_room()` を大物ロードの直前に置いた。統合メモリ機でのみ働き、
  `MemAvailable` が足りなければ **OOM killer に殺される前に** RuntimeError で落ちる。
  発想は `H3_GROUP_OFFLOAD_MIN_RAM_GB` (group モードの CPU 常駐ロード前ガード) と同じ。
  見積りは保守側に倒して bf16 チェックポイントの実体 66.71GB を使っている
  (**36% で kill された理由が未確定**なため。逐次量子化なら 7.6GB 相当のはずで説明が
  つかない。ページキャッシュか HF xet の実体化が疑わしいが未確認)。

#### 統合メモリ機では VAE の CPU パークが逆効果になる

`bnb-4bit` モードは VAE 対 (11GB) を CPU にパークし、必要な位相だけ GPU へ移す。
96GB機ではこれが**必須**だった (transformer 66.3 + TE-nf4 21.0 + VAE 11.0 = 98.5GB が
カードの ~95.6GB を超えて実際に OOM した)。**統合メモリ機ではこれが3重に裏目に出る**:

1. CPU へ退避しても同一プールなので **1バイトも空かない**
2. `.to(DEVICE)` は 11GB の実コピーを作り、CPU 側の実体も生きたままなので、
   デコード位相の実圧が **+11GB 増える**(専用VRAMの箱なら別プール間の移動で相殺される)
3. 毎リクエスト 11GB×2 のコピーと `empty_cache()` の時間を払う

そこで `H3_VAE_RESIDENT`(既定 `"auto"`)を入れ、統合メモリ機では VAE を GPU に
常駐させたままにした(`_vae_to_gpu`/`_vae_to_cpu` が no-op になる)。判定は
**ハードウェア特性**(`_is_unified_memory()`)で行う — 変動する空き容量で決めると
起動ごとに挙動が変わって再現性が無くなるため。統合メモリでない箱では従来どおり
CPU パークで、挙動は変わらない。

GB10 の収支(TE nf4 21.0 + transformer bf16 66.3):

| 位相 | CPU パーク時 | VAE 常駐時 |
|---|---|---|
| エンコード | 87.3 | 98.3 |
| デノイズ | 93.9 | 104.9 |
| デコード | 103.59 **+ CPU側の実体 11** | **103.59** |

定常状態は 11GB 増えるが、**ピークはむしろ下がる**。

#### 統合メモリ機では bf16 TE (`H3_TE_QUANT=none`) が成立する

`none` モードは2つの 66GB モデルをリクエストごとに入れ替える設計 (§ `core/runner.py`
冒頭 docstring) なので、**同時常駐しない**:

| 位相 | 常駐 | 計 |
|---|---|---|
| リクエスト間 | transformer 66.3 + VAE 11 | 77.3GB |
| エンコード | TE 66.71 + VAE 11 (transformer は解放済み) | 77.7GB |
| デノイズ / デコード | transformer 66.3 + VAE 11 | 77.3GB |

ピーク約 78GB は GB10 の 119.6GB に対し 40GB 以上余る。**量子化を一切挟まないので
初回ロードのピーク問題も無い**。代償は速度で、リクエストごとに TE と transformer を
1回ずつ載せ替える (96GB機の実測で TE 約37秒 + transformer 約26秒)。GB10 では
2モデル計 133GB がRAM 119.6GB を超えるためページキャッシュに両方は載らず、
**毎回 NVMe から読み直しになるので上記より悪化する見込み** (要実測)。

### 5.3 容量から導かれるモード(2026-08-10 全面更新 / 2026-08-12 に 48GB 行を実測で更新)

**前提が3つ変わった**(いずれも同日の実測): ①投影TE NF4 の常駐 **3.11GB**(32B nf4 削除版
17.45GB の代替。制約: `<d>` タグ不可=音声参照で代替・細部は近似。ref2va の vision 経路は
2026-08-11 に目視検証済み)、②デコード位相が **7.53GB**(video VAE fp16 + uint8修正後。fp32 でも 14.2GB)、
③デノイズは 34.03+6.6=**40.6GB** のまま。この3つで下表を引き直した。
**「実測済」以外は導出値**であることに注意。

**単騎(2nd GPU なし)**

| 容量(実効) | 32B TE 経路 | 投影TE(NF4)経路 |
|---|---|---|
| 96GB | bf16 TE+transformer 常駐(実測済) | 不要(余裕が有り余る) |
| 48GB(~49.8) | `H3_LOWVRAM=1` 毎回載せ替え(実測済) | **全部同時常駐が成立**: 導出 3.11+34.03+7.53=**44.7GB** に対し、96GB機での実測ピークは **45.6GB**(t2va 5秒。§0 ②)で余裕 ~4.2GB。ガード改修は完了(下記)。**48GB 実機での確認は未実施** |
| 32GB(~30.5) | `group`(nf4 21+ブロック1.4+活性化6.6=29 でぎりぎり) | `group` で余裕(TE込み ~11.1GB) |
| 24GB(~22.4) | `group`+`H3_TE_PRUNE=1` 必須(実測済の従来床) | `group` で余裕(~11.1GB) |
| **16GB(~15.2)** | **不可**(TE 17.45 が載らない) | **実測済(2026-08-11)**: 実 RTX 4060 Ti 16GB **単体**で t2i/t2va/ref2i/i2va/音声参照/768×1344 が全部完走。ピーク 7.4〜15.2GB(t2va デノイズ 11.4GB、i2va 9.41GB、音声参照 11.96GB、768×1344 は nvidia 実測 15.2GB)。従来「不可」だった16GBに道が開いた |

> **16GB 単体の起動(2026-08-11 実測)**: `H3_LOWVRAM=group H3_TE_PROJ=... H3_VIDEO_VAE_FP16=1
> H3_ATTN_BACKEND=default`(sage は sm_120 専用ビルドのため SDPA に戻す。TE_DEVICE なし=
> 投影TE同居)。導出 11.1GB は実測 t2va デノイズ 11.4GB でほぼ的中。参照系(i2va/音声参照/
> 768×1344)も同カードで全部動き、768×1344/5秒(実ピーク 15.2GB)が実質上限。**速度は
> 96GB機の2番スロットが Gen3 x4 のため 16.5〜51s/step**(カードの性能ではなくスロットの値。
> Gen4 x16 なら転送 ~1/8)。**FBC は int8+SDPA 軌道では0スキップ**(閾値調整は未検証)。

> **48GB 単騎(ガード改修は完了、2026-08-11/12)**: `H3_KEEP_TRANSFORMER` の import 時ガードは
> かつて `H3_TE_DEVICE` を必須にしていた(32B TE 前提の設計)が、**`H3_TE_PROJ` でも通る**ように
> 緩和され、投影TEを**同じGPUに同居**させたまま transformer を常駐させられるようになった
> (3.11 + 34.03 = 37.1GB、デノイズ活性化 6.6GB を足しても 43.7GB で予算内)。さらに条件1も
> 「`H3_LOWVRAM` が `group` でないこと」へ緩和され、plain モードでも使える(§5.5)。

**+2nd GPU(`H3_TE_DEVICE` で TE を外出し)**

| main | 2nd GPU の要件 | 成立するもの |
|---|---|---|
| 48GB | 20GB(32B pruned 17.76 実測) | **48GB機で実測済**: t2i **9.7s** / t2va 5秒 **44.2s**(turbo 4steps)。ref2va は 2nd 24GB 必要(上の §3 の囲みも参照) |
| 48GB | **8GB級**(投影NF4 3.11) | 同じ常駐構成をずっと安いカードで(導出) |
| 32GB | 20GB(32B)/ 8GB級(投影NF4) | `group` のまま余裕が増えるだけ。**非group は 2nd があっても不可**(デノイズ 40.6 > 30.5 は main 側の問題) |
| **8GiB×2** | **8GiB(投影NF4 3.11+ε)** | **実測済(2026-08-11、バラスト模擬)**: 計算側 8GiB + TE側 8GiB で t2i/t2va 5秒 768²/ref2i まで完走(t2i ピーク 6.4GB、t2va ピーク **7.23GB**、ref2i ピーク 6.69GB)。**動画の参照系(i2va/音声参照)は不可**: 参照トークンで系列が伸び、最短の 768²/5秒でもデノイズ活性化が入らない(i2va の必要量は実測 9.41GB) |
| 24/16GB | **6〜8GB級**(投影NF4) | main は group 専念(ブロック+活性化 ~8GB)。TE を外に出すと main の下限がさらに下がる |

**8GiB×2 の境界(2026-08-11 実測)**: 静止画参照(ref2i)までは成立するが、**動画の参照系は
参照トークン分の VRAM 加算で不可**。t2va 7.23GB に対し i2va は 9.41GB を要し(参照で +2.18GB)、
音声参照・768×1344 も 8GiB 予算を超える。これらは 16GB 単体でのみ動く。

ref2va は全段で注意: 32B TE では参照エンコードに **+3.22GB以上**(実測)が TE 側 GPU に
必要。投影TE の vision 経路は **2026-08-11 に目視検証済み**(顔・髪・服・小物まで反映、32B
正解と同水準。「潜伏バグ2件」修正後)。

### 5.4 TE を別GPUに置く場合(`H3_TE_DEVICE`)

TE用GPUに必要な容量は用途で変わる。

現行 20GB カードの**実効予算は約 19.7GB**(カタログ 21.47GB − 単位差 0.5 − 非PyTorch ~1)。

| TE | 必要量 | 成立するカード |
|---|---|---|
| 32B pruned nf4 / t2va系 | 17.76GB(実測) | **20GB以上**(余裕 ~1.9GB) |
| 32B pruned nf4 / ref2va | **20.67GB以上**(TE 17.45 + 参照エンコード 3.22以上、実測) | **24GB以上**(20GBは 204MB 不足で OOM 実測。複数参照は24GBでも保証なし) |
| 投影4B bf16 | 8.88GB(実測)+ε | 12GB(余裕薄い)/ **16GB以上** |
| **投影4B NF4** | **3.11GB(実測)+ε** | **8GiB級で実測済(2026-08-11)**: 8GiB×2 の TE 側で常駐(t2i/t2va/ref2i 完走)。6GB級は導出。ref2va の vision 経路は目視検証済み |

**→ 32B TE で ref2va をやるなら実効 20.7GB 以上、つまりカタログ 22.2GB 以上。** 24GB カード
(実効 ~22.4GB)なら余裕 ~1.7GB で成立する見込み。これが RTX PRO 4000 Blackwell 24GB へ
交換する具体的な根拠(**容量だけが理由**で、PCIe 幅や世代は無関係 — §3 参照)。

ただし余裕 1.7GB は薄く、参照画像を 2枚以上にするとさらに要求が増える(実測では
2枚でも OOM した)。**24GB でも 32B TE の ref2va 複数参照は保証できない。**

**投影TE の場合はこの容量計算が当てはまらない**(3.11GB + vision 活性化)にもかかわらず、
コード側のガードは容量 24GB 未満を一律に拒否する。**要見直し・未修正**(§3 の囲み)。

### 5.5 `H3_KEEP_TRANSFORMER=1` — transformer 常駐のままデコード

**transformer を常駐させたままデコードする**構成。**このリポジトリで最も効いた1個のフラグ**で、
`H3_LOWVRAM=1` では毎リクエストの transformer ロード(14.8〜32.7s)が、plain モードでは
デコード窓ごとの解放+再ロード(**11.9〜12.3s/リクエスト**)が、それぞれ消える。
5.3 の不等式どおり fp32 デコードでは入らないが、`H3_VIDEO_VAE_FP16=1` を使うと入る:

```
transformer 34.3 + デコードピーク 16.29 = 50.6GB  > 48GB   ← 不可(fp32 VAE)
transformer 34.3 + 11.4 (H3_VIDEO_VAE_FP16=1)   = 45.7GB  < 48GB  ← 導出予測
```

`core/runner.py` の `H3_KEEP_TRANSFORMER=1` として実装済み。成立条件は import 時ガードで
強制する(3つとも必須、欠けたら `RuntimeError`。**2026-08-11 と 08-12 の2回にわたって
条件1・2が緩和された**ので、古い「3条件」の記述に注意):

1. **`H3_LOWVRAM` が `group` でないこと**(`1` でも `0`/plain でも可 — 2026-08-12 に緩和)。
   `group` だけが対象外で、あちらは transformer を CPU 常駐させブロック単位で往復させる
   別設計だから無関係
2. **`H3_TE_DEVICE` または `H3_TE_PROJ` のいずれか**(2026-08-11 に緩和)。要求の本質は
   「**32B TE を計算用GPUに同居させないこと**」で、同居させると**デコード位相ではなく
   エンコード位相が先に破綻する**(TE-nf4 17.45GB + 常駐 transformer-int8 34.3GB =
   51.75GB > 実効予算 49.8GB)。投影TE(`H3_TE_PROJ`)は NF4 で 3.11GB しかないので
   同居しても 3.11+34.03=37.1GB、活性化 6.6GB を足しても 43.7GB で予算内 — だから
   別GPUを要求する理由がなく、免除される
3. `H3_VIDEO_VAE_FP16=1`(fp32 では上記のとおり 50.6GB で入らない)

既定(`H3_KEEP_TRANSFORMER=0`)は挙動不変。

**このフラグが触るのは `transformer`(t2va/fl2va/t2i 側)だけ**で、`transformer_ref`
(ref2va/ref2i 側)には一切関与しない。コード上、解放をスキップするのは `generate()` と
`generate_still_batch()` の「入口」と「デコード窓」の計4箇所で、いずれも
`self._free_transformer()` のみを対象とする。`self._free_transformer_ref()` はこれらの
入口でも**無条件に呼ばれる**(t2va リクエストに入るとき ref 側は必ず落ちる)。
`transformer_ref` の常駐可否を決めているのは別のフラグ **`H3_TRANSFORMER_BOTH_RESIDENT`**
(`H3_TRANSFORMER_QUANT=int8` のとき自動的に真)であり、plain + int8 では ref2va 完了時に
t2va 用 `transformer` を戻す設計(**約13秒**、実測)のため**両方の transformer が常駐**して
ピークが跳ねる(96GB機実測 74.3GB、t2va に戻ったときのピーク 77.3GB)。
**48GB カード1枚で運用するなら、t2va 系と参照系はプロセスを分けること。**

#### plain モード(`H3_LOWVRAM=0`)での等価性・効果(96GB機、2026-08-12 実測)

条件1の緩和で plain モードにも適用できるようになった。**追加の実装は不要**だった —
解放をスキップする分岐は元から共通で、復元側の `_ensure_transformer` は冪等だから。

- **同一seed の PNG が MD5 完全一致**(`596a718e4b5cf9a0b907d2ec479225d2`)。解放の停止は
  数学的に無影響で、**同じ絵が 19.58s → 7.40s(2.6倍)**で出る(int8 単騎・t2i 768²・turbo)
- 同構成のピークは 39.8GB(KEEP=0)→ **45.6GB**(KEEP=1)。解放をやめた分だけデコード位相に
  transformer が乗るため増える(**位相ごとの内訳は未計測**)

#### 構成別の実測(96GB機、turbo 4steps、768²、定常値)

| 構成 | t2i | t2va 5秒 | ピーク | GPU |
|---|---|---|---|---|
| bf16 + TE@GPU1 + 解放停止 | **6.89s** | **26.8s** | 74.2GB + 3.2GB | 2枚 |
| bf16 単騎(TE も GPU0)+ 解放停止 | 7.08s | 27.04s | 77.3GB | 1枚 |
| **int8 単騎 + 解放停止(実用最適)** | **7.40s** | **28.13s** | **45.6GB** | 1枚 |
| int8 + `H3_LOWVRAM=1` + KEEP + TE@GPU1 | 7.65s | 28.56s | 42.5GB | 2枚 |
| int8 単騎・解放あり(KEEP=0) | 19.58s | — | 39.8GB | 1枚 |
| bf16 + TE@GPU1・解放あり | 19.9s | 40.0s | 68.9GB | 2枚 |

bf16 のデノイズは int8 より速い(t2i 2.05〜2.07s vs 2.39〜2.40s、t2va 14.05s vs 14.81s)が、
**77GB 級のカードが要る**。45.6GB で 7% 落ちの int8 とどちらを取るかは持っているカード次第。

#### モード別の到達点(96GB機・int8 単騎 + 解放停止・turbo 4steps・768²・定常値)

| モード | 単発 | 連番なら1件あたり | デノイズ | ピーク |
|---|---|---|---|---|
| t2i 768² | 7.40s | 7.9s(0.94倍 = 効果なし) | 2.40s | 45.0GB |
| t2va 5秒 | 28.13s | (バッチAPIなし) | 14.94s | 45.6GB |
| ref2i 768² | 79.3s | 47.0s(1.69倍) | 7.8s | 45.4GB |
| i2va 5秒 | 103.1s | 75.0s(1.37倍) | 22.0s | 45.9GB |

30ステップ版(turbo なし)は t2i 27.42s / t2va 155.0s(FBC なし)、21.41s / 121.5s(FBC あり)、
ref2i 148.4s / i2va 290.3s。

#### 初回コスト(定常値には含まれていない)

上の表は**すべて定常値**(2回目以降)である。プロセスあたり1回だけ、次の初回コストがかかる:

- **起動時に transformer + VAE を常駐させるのに約50秒**
- **参照系だけは初回リクエストで別途 `transformer_ref` をコールドロード(+55秒)**。
  `transformer_ref` は起動時ではなく最初の参照リクエストでロードされるため、
  ref2i の初回は 134.7s、以降が定常 79.3s になる

#### i2va 103.1s の内訳(96GB機、ログのタイムスタンプ)

| 位相 | 時間 | 備考 |
|---|---|---|
| 参照のビジョンエンコード | **約47s** | **最大の律速**。4B 投影TE でも縮まない |
| デノイズ(4steps) | 22.0s | |
| デコード + VAE 往復 | 約10s | |
| 参照のVAEエンコード | 約6s | |
| 末尾で t2va 用 transformer を再ロード | 約13s | 連続 ref2va では純粋な無駄(**未修正**) |

#### 48GB機での実機 E2E 実測(2026-08-09、`H3_LOWVRAM=1` 経路)

以下は 48GB GPU0 + 20GB GPU1 の箱で、`H3_LOWVRAM=1 H3_TE_PRUNE=1 H3_TE_DEVICE=cuda:1
H3_VIDEO_VAE_FP16=1 H3_KEEP_TRANSFORMER=1`(=32B pruned TE を2枚目GPUへ)で測ったもの。
**現在の最速ではない**が、48GB 級の実機での唯一の実測なのでそのまま残す:

- transformer(int8) は初回リクエストで1回だけロード(32.0s)。以降のリクエストで再ロード
  なし(サーバーログで確認)
- t2i turbo 4steps 定常: **9.7s/枚**(denoise 4.32s、decode 1.5s)。peak VRAM 41.97GB
  (デノイズ時)
- t2i steps=30 定常: 51.1s(denoise 45.7s)。peak 41.97GB
- t2va 5秒 turbo 4steps: **44.2s**(denoise 26.05s、decode 10.81s)。**peak VRAM
  44.15GB = デコード位相**(transformer 34.03GB 常駐 + fp16 デコード)。上記の導出予測
  45.7GB に対し実測 44.15GB、カタログ 48.9GB に対し余裕 ~4.8GB
- nvidia-smi 実測ピーク 42,620 MiB(1秒サンプリング。瞬間ピークは torch 計測の
  44.15GB が正)
- 同一seed 出力等価性: フラグOFFのベースライン(他は同条件)と PNG MD5 **完全一致**
  (seed=11、md5 `665eadddea8f34298a1b5b89e69d4bd0`)。ベースライン側は total 63.27s
  (transformer ロード込み)/ peak 36.4GB
- 高速化の系譜: t2i turbo 157s(08-07朝)→ 83.2s(`H3_TE_PREQUANT`)→ ~35s
  (`H3_TE_DEVICE`)→ **9.7s**(`H3_KEEP_TRANSFORMER`)。t2va 5s(768²):
  turboなし30steps **351.4s**(48GB素の構成)→ turbo 143s → 60.5s → **44.2s**(8.0倍)

### 5.6 導出式の検算(全ケースで実測と一致)

上の式に既知の実測を通したところ、成否の判定が**すべて実測と一致**した(導出値そのものは
±1GB 程度ずれる — 保守側に外れることもあれば、下の plain+KEEP のように実測が 0.9GB 上回る
こともある。**可否の判定には使えるが、余裕が 1GB を切る構成は実測するまで断言しないこと**)。

| ケース | 必要 / 実効予算 | 判定 | 実測 |
|---|---|---|---|
| TE用GPU 20GB・t2va | 17.76 / 19.97 | OK | ○ 成立 |
| TE用GPU 20GB・ref2va | 20.67 / 19.97 | NG | ○ OOM |
| TE用GPU 24GB・ref2va | 20.67 / 24.27 | OK | (未実測・期待値) |
| GPU0 48GB・デノイズ | 40.60 / 49.81 | OK | ○ peak 40.89 |
| GPU0 48GB・TE常駐のままデノイズ | 58.05 / 49.81 | NG | ○ だから毎回入れ替えている |
| GPU0 48GB・transformer常駐のままデコード | 50.29 / 49.81 | NG | (fp32 VAEでは未実装のまま) |
| 同上 + video VAE fp16(`H3_KEEP_TRANSFORMER=1`) | 45.70 予測 / 49.81 | OK | ○ 実測 44.15GB(§5.5、2026-08-09、48GB機) |
| 単騎・投影TE同居で transformer も常駐(plain + KEEP) | 44.7 予測 / 49.81 | OK | ○ 実測 45.6GB(§5.5、2026-08-12、96GB機。**48GB 実機は未確認**) |
| 同一プロセスで t2va 系と参照系を混在 | 74.3 実測 / 49.81 | NG | ○ 両 transformer が常駐(96GB機。t2va 復帰時 77.3GB) |
| GB10・投影TE + bf16 transformer 常駐で **fp32** デコード(plain + KEEP) | 85.70 予測 / 119.30 | OK | (2026-08-12。48GB級で fp16 を強制していたガードを実測比較へ移した動機) |
| 16GB単体(投影TENF4同居 group)・t2va デノイズ | 11.1 予測 / ~15.2 | OK | ○ 実測 11.4GB(2026-08-11) |
| 8GiB×2(投影TE外出し)・t2va | 7.23 / ~7.1 | OK(ぎりぎり) | ○ 実測 7.23GB(2026-08-11) |
| 8GiB×2・i2va(参照あり) | 9.41 / ~7.1 | NG | ○ OOM(参照トークンで +2.18GB) |

**参照トークンによる VRAM 加算(2026-08-11 実測、16GB単体)**: t2va(参照なし)7.23GB に対し、
i2va(画像参照)9.41GB、音声参照 11.96GB、768×1344/5秒 13.37GB(nvidia 実測 15.2GB)。
参照系は参照トークンの分だけ系列が長くなり、デノイズ活性化が増える。8GiB×2 では静止画参照
(ref2i ピーク 6.69GB)までが上限で、動画の参照系はこの加算で予算を超える。

**GPU を替えたらこの式に新しい容量を入れて再導出すること。** 表を覚え直す必要はない。

## 6. 実行中に確認する方法

```bash
# runner が今どう思っているか
curl -s http://127.0.0.1:8611/api/status | python3 -m json.tool | grep -E "loaded|on_gpu|peak|allocated"

# 実際のGPU占有(プロセス単位)
nvidia-smi --query-compute-apps=gpu_uuid,pid,used_memory --format=csv

# ロード/解放の履歴(これが一番確実)
grep -E "transformer (loaded|freed)|text_encoder|vae/audio_vae ->" logs/server.log | tail -20
```

ログの各行に `gpu={'allocated_gb':..., 'peak_gb':...}` が付いているので、**推測せずここを読むこと**。§0 の表もこのログから起こしている。
